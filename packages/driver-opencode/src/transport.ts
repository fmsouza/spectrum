// Placeholder transport types — the real zod schema + injected port arrive in Task 2 (RED→GREEN).

/** Stub envelope — replaced by the zod-derived union in Task 2. */
export type OpencodeEvent = { readonly type: string }

/** Stub — replaced in Task 2. */
export type OpencodeClient = unknown

/** Stub — replaced in Task 2. */
export type OpencodeServer = unknown

/** Stub — replaced in Task 2. */
export type OpencodeConnect = unknown

/** Stub — replaced in Task 2. */
export type OpencodeConnectConfig = unknown
